Ruby变量类型：本地变量、实例变量、全局变量、常量。
实例变量的开头加@，全局变量的开头加$，常量全部大写。

变量的数据类型：字符串、数字、数组、逻辑值、哈希、符号。
数字有fixnum、bignum和float。fix number小一些。
数组以用[]括起来，中间用，分割。
逻辑值只有true和false。
哈希是用{}括起来，key value前面要加：。例如：
Hash = {:name => “Monster”, :age => 20}
符号用来保存不怎么变化的字符串，前面要加：。

